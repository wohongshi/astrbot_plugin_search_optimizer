# 🔍 搜索结果优化器 — AstrBot 插件详情页

> **网页搜索 + 结果压缩 + 缓存加速，节省 70%+ Token**

---

## 一句话介绍

内置 `web_search` / `web_fetch` 工具，自动压缩搜索结果，支持缓存加速和小模型预处理，让每次搜索少烧 70% 以上的 Token。

---

## 🎯 解决什么问题？

AstrBot 的 Agent 搜索一次网页，返回结果动辄 **3000-8000 字符**，全部塞进上下文交给主力模型处理。问题：

| 痛点 | 影响 |
|------|------|
| 搜索结果太长 | Token 消耗飙升，费用高 |
| 噪声太多 | 导航栏、广告、推荐、热搜榜全混在里面 |
| 重复搜索 | 同一个问题问两次，搜两次，烧两倍 Token |
| 结果不相关 | 境外服务器搜中文人名被拆字，返回一堆垃圾 |

**本插件的解法**：拦截 → 压缩 → 缓存 → 注入，一条龙。

---

## ✨ 核心功能

### 🔧 自包含搜索工具

不需要安装其他搜索插件，开箱即用：

| 工具 | 功能 | 技术栈 |
|------|------|--------|
| `web_search` | Bing 搜索 + 自动抓取详情页 | aiohttp + BeautifulSoup + trafilatura |
| `web_fetch` | 抓取指定 URL 内容，支持 JS 渲染 | aiohttp + trafilatura + DrissionPage |

**搜索特性：**
- 多关键词并行搜索，结果自动合并去重
- 搜索结果相关性检测，不相关时自动去虚词重搜
- 自动抓取前 3 个结果的详情页正文
- 5 分钟短期缓存，相同关键词秒回

**抓取特性：**
- 静态页面：aiohttp 快速抓取，trafilatura 智能提取正文
- JS 渲染页面：自动降级 DrissionPage + 系统 Chromium
- 多 URL 并行抓取
- 内网地址安全校验，拒绝访问私有网络

### 📦 两阶段压缩

```
原始搜索结果（5000 字）
  ↓
第一阶段：规则提取（零 LLM）
  ├─ 去除 URL、错误信息、版权信息、导航噪声
  ├─ 去重相似摘要（Jaccard 相似度）
  ├─ 智能截断（在句号/段落处断开）
  └─ → 约 1500 字（压缩率 ~70%）
  ↓
第二阶段：小模型精简（可选）
  ├─ 对规则提取后的内容做归纳总结
  ├─ 不使用人格配置（避免小模型输出异常）
  └─ → 约 800 字（压缩率 ~85%）
  ↓
精炼内容交给主力模型
```

**两种模式：**

| 模式 | 配置 | 压缩率 | 额外成本 |
|------|------|--------|----------|
| 纯规则提取 | 预处理模型留空 | ~70% | 零 |
| 规则 + 小模型 | 配置低成本模型 | ~85% | 极低（小模型处理量很小） |

> 💡 即使配置了小模型，规则提取也会先运行。小模型处理的是精简后的短文本，不是原始长文，效果远好于直接压缩原文。

### ⚡ 缓存加速

开启「优化网络搜索」后：

```
用户提问 → 检查缓存 → 命中 → 跳过搜索，直接注入上下文（0 Token 搜索成本）
          → 未命中 → 正常搜索 → 压缩 → 缓存 → 回答
```

- 持久化缓存，重启不丢失
- 时间敏感词检测：包含「今天/最新/最近」等词的查询自动加入日期标记，避免返回过期缓存
- LRU 淘汰：缓存超限时自动清理命中次数最少 + 最久未用的条目
- 可配置缓存天数（1/3/7 天）和条目上限

### 🤖 小模型直接回答（可选）

开启后，缓存命中时用预处理模型**直接回答**用户问题，主力模型完全不参与：

```
用户提问 → 缓存命中 → 小模型基于缓存摘要生成回答 → 直接返回
                      （主力模型 0 Token 消耗）
```

适合事实性问题（"XXX 是谁"、"今天天气"等）。

---

## 📊 效果对比

以一次典型搜索为例：

| 指标 | 无插件 | 纯规则提取 | 规则 + 小模型 |
|------|--------|-----------|--------------|
| 搜索结果字符数 | 5000 | 1500 | 800 |
| 主力模型输入 Token | ~3300 | ~1000 | ~530 |
| 额外 LLM 调用 | 0 | 0 | 1（小模型，极低成本） |
| **Token 节省** | — | **~70%** | **~85%** |

多次搜索场景下，缓存命中后 Token 消耗趋近于零。

---

## ⚙️ 配置说明

在 AstrBot WebUI → 插件管理 → 搜索结果优化器 中配置：

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 预处理模型 | 选择 | 空 | 低成本 LLM，用于摘要压缩。留空使用纯规则提取 |
| 优化网络搜索 | 开关 | 关 | 命中缓存时跳过搜索，直接注入上下文 |
| 小模型直接回答 | 开关 | 关 | 缓存命中时用预处理模型直接回答（需先配置预处理模型） |
| 缓存相似度阈值 | 0.5-0.8 | 0.75 | 查询与缓存的字符相似度超过此值视为命中 |
| 缓存保留天数 | 1/3/7 | 3 | 过期自动清理 |
| 缓存条目上限 | 数字 | 200 | 超限时 LRU 淘汰 |
| 摘要最大字符数 | 数字 | 1500 | 控制压缩后长度 |
| 搜索/抓取超时 | 秒 | 30 | 单次请求超时时间 |

### 预处理模型推荐

| 模型 | 特点 | 推荐度 |
|------|------|--------|
| GLM-4-Flash（智谱） | 免费额度大，速度快 | ⭐⭐⭐⭐⭐ |
| DeepSeek-Flash | 便宜，中文好 | ⭐⭐⭐⭐ |
| Qwen-Turbo（通义） | 阿里云，稳定 | ⭐⭐⭐⭐ |
| Gemini Flash | Google，免费额度 | ⭐⭐⭐ |

> 不建议用太小的模型（如 270M 参数），可能返回空内容。

---

## 🎮 使用方式

### 自动工作

插件加载后自动生效，无需手动操作：

1. LLM 调用 `web_search` 搜索 → 插件自动拦截并压缩
2. LLM 调用 `web_fetch` 抓取 → 插件自动拦截并压缩
3. LLM 通过 `astrbot_execute_shell` 执行 curl 搜索 → 插件自动识别并压缩
4. 开启「优化网络搜索」后，相似查询自动注入缓存

### 管理员指令

| 指令 | 说明 |
|------|------|
| `搜索优化器` | 查看运行统计（模式、模型、缓存命中率、压缩率、节省 Token 数） |
| `清除缓存` | 清空全部搜索缓存 |
| `优化开启` / `优化关闭` | 开关缓存加速 |
| `小模型开启` / `小模型关闭` | 开关小模型直接回答 |

---

## 📦 安装

### 系统要求

- Python >= 3.10
- AstrBot >= 4.16
- （可选）系统 Chromium — 用于 JS 渲染页面抓取

### 方式一：WebUI 插件市场

在 AstrBot WebUI 的插件管理页面搜索 `astrbot_plugin_search_optimizer` 安装。

### 方式二：手动安装

```bash
cd AstrBot/data/plugins
git clone https://github.com/wohongshi/astrbot_plugin_search_optimizer.git
pip install -r astrbot_plugin_search_optimizer/requirements.txt
```

### 依赖

```bash
pip install aiohttp beautifulsoup4 trafilatura
# 可选：JS 渲染页面兜底
# pip install DrissionPage
```

---

## 🔧 工作原理

```
用户提问
  ↓
LLM 判断需要搜索
  ├─ 调用 web_search（插件内置工具）
  ├─ 调用 web_fetch（插件内置工具）
  └─ 调用 astrbot_execute_shell 执行 curl（插件也能识别）
  ↓
插件拦截工具返回结果
  ├─ 搜索结果相关性检测
  │   └─ 不相关 → 去虚词重组关键词 → 重搜
  ├─ 自动抓取前 3 个结果的详情页
  └─ 返回 摘要 + 详情
  ↓
第一阶段：规则提取
  ├─ 去除 URL、错误信息、版权、导航噪声
  ├─ 去重相似摘要
  ├─ 智能截断（在句号/段落处断开）
  └─ → 压缩率 ~70%
  ↓
第二阶段：小模型精简 [可选]
  ├─ 对规则提取后的内容归纳总结
  ├─ 不使用人格配置（system_prompt 为空）
  └─ → 压缩率 ~85%
  ↓
压缩后内容交给主力模型
  ↓
主力模型基于精炼内容回答用户
```

---

## 🛡️ 安全性

- **URL 校验**：拒绝内网地址、私有 IP、非法协议
- **错误处理**：工具调用失败不崩溃，自动降级
- **缓存隔离**：独立缓存目录，不影响其他插件
- **超时控制**：搜索/抓取可配置超时，防止卡死

---

## 📝 更新日志

### v6.1.0（最新）
- ✨ 新增 `skills/` 目录，技能在 WebUI Skills 页面可见
- ✨ 新增缓存相似度阈值配置（0.5-0.8）
- ✨ 支持拦截 `astrbot_execute_shell` 的搜索结果
- 🔧 修复 `on_agent_done` 和 `on_llm_tool_respond` 双重处理
- 🔧 DrissionPage 改为可选依赖
- 🔧 缓存模糊匹配改用字符级 Jaccard 相似度

### v6.0.0
- 自包含 `web_search` + `web_fetch` 工具
- 移除对其他搜索插件的依赖
- 两阶段压缩：规则提取 + 小模型精简
- LRU 缓存淘汰
- 搜索结果相关性检测

---

## 📄 License

MIT

## 🔗 链接

- [GitHub 仓库](https://github.com/wohongshi/astrbot_plugin_search_optimizer)
- [AstrBot 官方文档](https://docs.astrbot.app)
- [插件开发指南](https://docs.astrbot.app/dev/star/plugin-new.html)
- [AstrBot 社区](https://astrbot.app)
